# nix-otter-compat

Just cd into the repo and run "nix build".
Nix handles the toolchain and environment.
The assembled binary will be found under result/program.mem

I have provided test_all.s, but you can replace it with any of the other example programs found on Canvas and it will still build.