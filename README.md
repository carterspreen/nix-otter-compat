# nix-otter-compat
Build environment for RISC-V assembly. Binary compatible with files provided on Canvas for CPE333. Uses Nix for reproducable builds.

Just cd into the repo and run "nix build".
Nix handles the toolchain and environment.
The assembled binary will be found under result/program.mem